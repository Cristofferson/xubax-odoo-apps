<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Odoo 19 groups hang off a res.groups.privilege, which is what
             renders the three-way selector on the user form. -->
        <record id="res_groups_privilege_analitix" model="res.groups.privilege">
            <field name="name">Analitix</field>
            <field name="sequence">30</field>
            <field name="category_id" ref="base.module_category_sales_point_of_sale"/>
        </record>

        <!-- Reads the store's numbers: the salesperson, and the owner who only
             wants the dashboard. No configuration, no credentials, no biometrics. -->
        <record id="group_user" model="res.groups">
            <field name="name">User</field>
            <field name="sequence">10</field>
            <field name="privilege_id" ref="res_groups_privilege_analitix"/>
            <!-- Implies the internal-user group, as most Odoo app groups do.
                 Without it a "salesperson" could exist who has no access to
                 Discuss, and the chat channel would fail with a confusing
                 permissions error at the moment an alert fired. -->
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
            <field name="comment">See this store's traffic and conversion dashboards.</field>
        </record>

        <!-- The implementer. Task 982 asks for a technical dashboard separate
             from the owner's commercial one, and task 983 lists this role
             explicitly; defining it now means the person who keeps the cameras
             alive does not have to be handed the customer's sales figures to do
             that job. Phase 7 extends the hierarchy above these three, it does
             not replace this split. -->
        <record id="group_technician" model="res.groups">
            <field name="name">Technician / Implementer</field>
            <field name="sequence">20</field>
            <field name="privilege_id" ref="res_groups_privilege_analitix"/>
            <field name="implied_ids" eval="[(4, ref('group_user'))]"/>
            <field name="comment">Device health, ingest diagnostics and the job queue, without access to sales figures.</field>
        </record>

        <!-- Configures the store: doors, thresholds, staff enrolment, kill switch. -->
        <record id="group_manager" model="res.groups">
            <field name="name">Manager</field>
            <field name="sequence">30</field>
            <field name="privilege_id" ref="res_groups_privilege_analitix"/>
            <field name="implied_ids" eval="[(4, ref('group_user')), (4, ref('group_technician'))]"/>
            <field name="comment">Configure stores, doors and devices, enrol staff signatures and use the capture kill switch.</field>
            <field name="user_ids" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        </record>

        <!-- Regional / district manager (phase 7).

             Sees the consolidated picture of the stores in their regions and
             compares them against each other. Deliberately does NOT imply
             Manager: task 983 is explicit that a regional does not need the
             fine operational detail of every branch, and handing them the
             configuration of forty stores is how a threshold gets changed in
             the wrong one at eleven at night.

             What actually bounds them is not this group but the regions on
             their user record — the group says what kind of screen they get,
             the region says which stores are in it. -->
        <record id="group_regional" model="res.groups">
            <field name="name">Regional Manager</field>
            <field name="sequence">25</field>
            <field name="privilege_id" ref="res_groups_privilege_analitix"/>
            <field name="implied_ids" eval="[(4, ref('group_user'))]"/>
            <field name="comment">Compare the stores of their regions side by side. No store configuration.</field>
        </record>

        <!-- Corporate / head office (phase 7).

             The whole chain, plus the two decisions nobody below may take: how
             far face recognition reaches between branches, and how far the
             watch list does. Those are guarded in code on analitix.brand.write,
             not only by this group's menu visibility — a control that lives in
             the UI is defeated by an import. -->
        <record id="group_corporate" model="res.groups">
            <field name="name">Corporate / Head Office</field>
            <field name="sequence">35</field>
            <field name="privilege_id" ref="res_groups_privilege_analitix"/>
            <field name="implied_ids" eval="[(4, ref('group_manager')), (4, ref('group_regional'))]"/>
            <field name="comment">The whole chain: cross-branch comparison, chain configuration, and the elevated decisions about how far recognition reaches.</field>
        </record>

        <!-- Security / compliance. The watch list, and nothing else.

             Deliberately NOT implied by Manager, which is why it sits outside
             the ladder the three groups above form. Someone's presence on a
             watch list is not commercial information, and a store manager who
             can read conversion figures has no business reading it by default.
             Task 983 names this role as the only one with access to phase 5;
             granting it is a separate, deliberate act by whoever runs the
             instance.

             It implies Analitix User only so the holder can navigate to a
             store at all — not so they can be handed the sales numbers with
             it. Nobody is in this group on install, including the
             administrator: an empty watch list with nobody able to open it is
             the correct state for a shop that has never asked for one. -->
        <record id="group_security" model="res.groups">
            <field name="name">Security / Compliance</field>
            <field name="sequence">40</field>
            <field name="privilege_id" ref="res_groups_privilege_analitix"/>
            <field name="implied_ids" eval="[(4, ref('group_user'))]"/>
            <field name="comment">Manage the watch list: add, confirm, review and remove entries, and receive match alerts. Not granted to anybody by default.</field>
        </record>

    </data>
</odoo>

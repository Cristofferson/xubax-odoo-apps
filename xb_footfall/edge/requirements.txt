# xb_footfall edge agent — Python deps (only needed for a bare-metal install;
# the Docker image already bundles ultralytics + opencv + torch CPU).
ultralytics>=8.3.0
opencv-python-headless>=4.9.0
PyYAML>=6.0
# Pillow: show the camera frame in the desktop configurator (configurator.py).
# It also ships as an ultralytics dependency; pinned here for a bare install.
Pillow>=10.0
